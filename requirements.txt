pyannote.audio>=3.1 # Or a more specific version if known to be stable/required
# torch and torchaudio are usually core ComfyUI dependencies,
# but listing them explicitly can be good practice if specific versions are needed.
# For now, relying on ComfyUI's environment for these.
# torch
# torchaudio
